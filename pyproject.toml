[project]
name = "regressor-project"
version = "0.1.0"
description = "LLM evaluation and regression detection prototype"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
    "httpx>=0.27.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "pytest-timeout>=2.0.0",
]
